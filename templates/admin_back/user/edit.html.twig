{% extends 'admin_back/baseback.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-body">
            <h5 class="card-title fw-semibold mb-4">Modifier</h5>
            <div class="card">
                <div class="card-body">
                    {{ include('admin_back/user/_form.html.twig', {'button_label': 'Modifier'}) }}

                    <a href="{{ path('back_user_index') }}" class="btn btn-warning">Retour a la liste </a>
                    <br> <br>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
