{% extends 'profile.html.twig' %}

{% block title %}Mon profil{% endblock %}

{% block body %}
    <h1>{{ app.user.prenom|default('') }} {{ app.user.nom|default('') }}</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
    <br><br>
    {{ form_end(form) }}

{% endblock %}
